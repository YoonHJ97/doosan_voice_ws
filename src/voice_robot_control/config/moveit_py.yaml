# moveit_py.yaml — MoveItPy(파이썬 MoveIt) 동작 설정
#
# 수업 중에는 건드릴 일이 거의 없습니다.
# 속도를 바꾸고 싶으면 launch 인자 vel_scale 을 쓰세요.

/**:
  ros__parameters:

    planning_scene_monitor_options:
      name: "planning_scene_monitor"
      robot_description: "robot_description"
      joint_state_topic: "/joint_states"
      attached_collision_object_topic: "/moveit_cpp/planning_scene_monitor"
      publish_planning_scene_topic: "/moveit_cpp/publish_planning_scene"
      monitored_planning_scene_topic: "/moveit_cpp/monitored_planning_scene"
      wait_for_initial_state_timeout: 10.0

    # 사용할 경로 계획기
    #   ompl : 장애물을 피해 돌아가는 범용 계획기 (관절 이동에 사용)
    #   pilz : 직선/점대점 같은 산업용 단순 경로  (좌표 이동에 사용)
    planning_pipelines:
      pipeline_names: ["ompl", "pilz_industrial_motion_planner"]

    # PlanRequestParameters 의 기본값 — 노드에서 덮어쓴다
    plan_request_params:
      planning_attempts: 1
      planning_pipeline: ompl
      planner_id: "RRTConnectkConfigDefault"
      planning_time: 3.0
      max_velocity_scaling_factor: 0.15
      max_acceleration_scaling_factor: 0.15
